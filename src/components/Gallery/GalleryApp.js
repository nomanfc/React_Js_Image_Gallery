import styles from "../../styles/gallery.module.css";

export default function GalleryApp() {
  return (
    <div className={styles.outerContainer}>
      <div className={styles.mainContainer}>GalleryApp</div>
    </div>
  );
}
